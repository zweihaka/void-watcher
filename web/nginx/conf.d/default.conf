upstream void_backend {
    least_conn;
    server app:8000;
    # server app2:8000; # if we have more instances
    # server app3:8000;
    keepalive 32;
}

limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

server {
    listen 80;

    keepalive_timeout       65;
    client_max_body_size    10m;
    client_body_timeout     12s;
    client_header_timeout   12s;
    send_timeout            10s;

    server_tokens off;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    # Frontend static
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;

    # Cache for static
        location ~* \.(js|css|png|jpg|svg|ico|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        }

    }

    # API - reverse-proxy to backend
    location /api/ {
        limit_req zone=api_limit burst=50 nodelay;
        limit_conn conn_limit 20;

        proxy_pass      http://void_backend;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host               $host;
        proxy_set_header X-Real_IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $scheme;
        proxy_set_header Connection         ""; # For keepalive
        
        # Proxy timeouts
        proxy_connect_timeout       5s;
        proxy_send_timeout          60s;
        proxy_read_timeout          60s;
        
        # Buffering responses
        proxy_buffering         on;
        proxy_buffer_size       8k;
        proxy_buffers           8 8k;

        # If our backend is down - trying to resolve the next one
        proxy_next_upstream error timeout http_502 http_503;

    }
    
    # Healthcheck endpoint for docker/balancer
    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
